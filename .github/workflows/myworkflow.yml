name: My workflow test
on: 
  issues:
    types: opened
  workflow_dispatch:

jobs:
  files:
    runs-on: ubuntu-latest
    steps:
      - name: filecreation
        run: |
          touch info.txt
          echo "Heipparalla" > info.txt
          cat info.txt
    

